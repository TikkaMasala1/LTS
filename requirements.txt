# Fallback for pip users. Authoritative dependency file: pyproject.toml
# Recommended install:  uv sync   (see README §1)
# Core
mcp>=1.2.0              # Model Context Protocol SDK (FastMCP + stdio client)
pydantic>=2.6
httpx>=0.27             # Ollama and Autotask API calls
python-dotenv>=1.0

# UI
streamlit>=1.35

# Live mode (optional) & tests
psutil>=5.9
pytest>=8.0
